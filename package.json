{
  "name": "mesh",
  "version": "1.0.0",
  "private": true,
  "description": "Mesh — Every digital claim deserves a verifiable Evidence Passport.",
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md,sol,json,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,sol,json,yml}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-solidity": "^2.3.1",
    "turbo": "^2.0.6"
  },
  "engines": {
    "node": ">=20"
  }
}
